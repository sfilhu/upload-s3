'use strict';

const AWS = require('aws-sdk');

module.exports.handle = async ({ Records: records }, context) => {
  try {
    records.map( record => {
      
    });
  } catch (err) {
    return err;
  }
};
